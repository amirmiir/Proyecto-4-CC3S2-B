# Variables de entorno para el gestor de procesos
# Copiar este archivo como .env y ajustar valores según necesidad

# Puerto del servicio web
PORT=8080

# Mensaje del servidor
MESSAGE="Servidor activo"

# Versión del release
RELEASE=v1.0.0

# Variables adicionales para monitoreo de redes
DNS_SERVER=8.8.8.8
TARGETS=localhost,example.com
CONFIG_URL=http://localhost:8080/config
TIMEOUT=30

# Directorios de trabajo
LOG_DIR=/tmp/gestor-logs
PID_DIR=/tmp
OUT_DIR=./out
DIST_DIR=./dist